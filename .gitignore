# Listado de archivos y carpetas que se deben excluir al realizar operaciones de subida con git.
# Excluded sfdx folders & files.
config
scripts
bin
secret
*.code-workspace
# Salesforce cache
.sfdx/
.localdevserver/
manifest/package-retrieve.xml
manifest/package-baseDeploy.xml
# LWC VSCode autocomplete
**/lwc/*.json
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Dependency directories
node_modules/
# Eslint cache
.eslintcache
# MacOS system files
.DS_Store
.vscode
# Windows system files
Thumbs.db
ehthumbs.db
[Dd]esktop.ini
$RECYCLE.BIN/
#pretti
.prettierignore
.prettierrc
# Excluded metadata.
force-app/main/default/certificate
force-app/main/default/connectedApps
# delta deploy
delta*/
#Se ignora sfdx-project.json para que no se versione
sfdx-project.json
#Se ignora package-lock.json para que no se versione
package-lock.json
# Se ignora carpeta de Einstein Bots porque se versionan en la carpeta data-config/EinsteinBots
force-app/main/default/bots